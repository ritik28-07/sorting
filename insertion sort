//insertion sort
#include<iostream>
using namespace std;
int main()
{
	int arr[]={5,4,7,1,3,9,6,11};
	int temp,n=8;int j,i;
	for(i=1;i<n;i++)
	{
		temp=arr[i];
		for(j=i-1;j>=0;j--)
		{
			
			if(arr[j]>arr[i])
			{
				arr[j+1]=arr[j];
			
			}
			else
			break;
	    }
	    arr[j+1]=temp;
	}
		for(int i=1;i<n;i++)
        cout<<arr[i]<<" ";
}
